// Header file for sort_the_trio

#ifndef SORT_H
#define SORT_H

//Helper function to swap elements 'array[i]' and 'array[j]' in array. 
void swap (int *array, int i, int j);

//Sorts the array of ints
void sort(int *array, int high);

#endif

