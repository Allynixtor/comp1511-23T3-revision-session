#include <stdio.h>
#include <string.h>
#include <ctype.h> 

#define MAX 4096

int main(void) {
    // Handle taking in input
    char str[MAX];
    fgets(str, MAX, stdin);

    // TODO: Write your code here:

    return 0;
}
